# Deletion Ledger

Content-free operational receipts only.

| Event ID | At | Session ID | Data class | Object IDs | Scope | Derived references handled | Known backup warning shown | Result |
|---|---|---|---|---|---|---|---|---|

Use stable `delete-<uuid>` IDs. Never record deleted labels, statements, source excerpts, transcript text, reasons, or filenames that reveal sensitive content.

If the user also asks to delete operational receipts, apply that choice to this ledger after required actions finish; do not claim an audit receipt remains when it does not.
