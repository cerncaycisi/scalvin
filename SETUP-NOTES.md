# Repo Setup Notes

This file helps the repo-level entry adapters find the generated workspace.

- workspace_path:
- companion_name: Scalvin
- default_language:
- bootstrap_status: not_started
- last_bootstrapped:

If a generated workspace exists and `profile.md` there is populated, repo entry files should hand off into that workspace instead of running bootstrap again.
